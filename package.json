{
  "name": "void-rift",
  "version": "1.0.0",
  "description": "VOID RIFT - A browser-based twin-stick shooter built with vanilla HTML, CSS, and JavaScript",
  "main": "index.html",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint '*.js' 'src/**/*.js' 'api/**/*.js' --ignore-pattern 'coverage/**' --ignore-pattern 'node_modules/**'",
    "lint:fix": "eslint '*.js' 'src/**/*.js' 'api/**/*.js' --ignore-pattern 'coverage/**' --ignore-pattern 'node_modules/**' --fix"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/astickleyid/shooter-app.git"
  },
  "keywords": [
    "game",
    "shooter",
    "browser-game",
    "twin-stick-shooter",
    "javascript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
